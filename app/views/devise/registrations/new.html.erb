<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-4">
        <h2 class="title is-2">Sign up</h2>
        <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= f.error_notification %>

            <div class="field">
              <p class="control">
                <%= f.input :email,
                        required: true,
                        autofocus: true,
                        wrapper: false,
                        input_html: { autocomplete: "email", type: "email", class: "input" },
                        label_html: { class: "label" } %>
              </p>
            </div>      
            
            <div class="field">
              <p class="control">
                <%= f.input :username,
                        required: true,
                        wrapper: false,
                        input_html: { autocomplete: "username", class: "input" },
                        label_html: { class: "label" } %>
              </p>
            </div>

            <div class="field">
              <p class="control">
                <%= f.input :password,
                        required: true,
                        wrapper: false,
                        hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                        input_html: { autocomplete: "new-password", type: "password", class: "input" },
                        label_html: { class: "label" },
                        hint_html: { class: "help" } %>
              </p>
            </div>
            
            <div class="field">
              <p class="control">
                <%= f.input :password_confirmation,
                        required: true,
                        wrapper: false,
                        input_html: { autocomplete: "new-password", class: "input" },
                        label_html: { class: "label" } %>
              </p>
            </div>
            

          <%= f.button :submit, "Sign up", class: "button is-info" %>
        <% end %>

        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</section>



