<h1><%= @user.username %>'s Events</h1>
<% @user.events.each do |event| %>
  <ul>
    <li><%= event.title %></li>
    <ul>
      <li><%= event.description %></li>
      <li><%= event.date %></li>
      <li><%= event.location %></li>
    </ul>
  </ul>
<% end %>
